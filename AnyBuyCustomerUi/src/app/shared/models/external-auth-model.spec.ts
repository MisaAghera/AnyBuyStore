import { ExternalAuthModel } from './external-auth-model';

describe('ExternalAuthModel', () => {
  it('should create an instance', () => {
    expect(new ExternalAuthModel()).toBeTruthy();
  });
});
